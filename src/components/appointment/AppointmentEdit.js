import React from 'react';
import { View, Text, Image } from 'react-native';
import colors from './../config/colors';

function AppointmentEdit(props) {

  return(
    <View>
      <Text>Appointment Edit Screen</Text>
    </View>
  )
}

export default AppointmentEdit;