import React from 'react';
import { View, Text, Image } from 'react-native';
import colors from '../../config/colors';

function AppointmentCreate(props) {

  return(
    <View>
      <Text>Appointment Create Screen</Text>
    </View>
  )
}

export default AppointmentCreate;