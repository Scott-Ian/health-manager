import React from 'react';
import { View, Text, Image } from 'react-native';
import colors from './../config/colors';

function AppointmentList(props) {

  return(
    <View>
      <Text>AppointmentList Screen</Text>
    </View>
  )
}

export default AppointmentList;